
provider_installation {
  ###filesystem_mirror {
  ###  path    = "/Users/buck/prefix/tf-localhost"
  ###  include = ["localhost/*/*"]
  ###}
  direct {
    exclude = ["localhost/*/*"]
  }
}


plugin_cache_dir = "$PWD/terraform/plugin"

# vim:ft=terraform:
